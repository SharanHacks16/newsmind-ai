import { useEffect, useState } from "react";
import axios from "axios";
import { FileBarChart, ImageOff } from "lucide-react";
const API_URL=import.meta.env.VITE_API_URL||"http://127.0.0.1:5000";
const graphs=[["Accuracy Comparison","accuracy_comparison.png"],["Confusion Matrix","confusion_matrix.png"],["Dataset Distribution","dataset_distribution.png"],["Word Cloud","wordcloud.png"],["Top Frequent Words","top_words.png"]];
export default function Analytics(){const[report,setReport]=useState("");const[error,setError]=useState("");useEffect(()=>{axios.get(`${API_URL}/api/report`).then((response)=>setReport(response.data.report)).catch(()=>setError("Run python evaluate.py to generate analytics assets."));},[]);return <section className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-sm font-semibold uppercase tracking-widest text-teal-300">Evaluation Dashboard</p><h1 className="mt-3 text-4xl font-bold text-white">Model analytics and report assets</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-slate-300">These visuals are generated by the local evaluation script and are ready for a college project report.</p></div><div className="flex items-center gap-2 rounded-lg border border-white/10 bg-white/[0.05] px-4 py-3 text-sm text-slate-300"><FileBarChart size={18} className="text-teal-300"/>Graphs from Flask backend</div></div><div className="mt-8 grid gap-6 lg:grid-cols-2">{graphs.map(([title,file])=><article key={file} className="rounded-lg border border-white/10 bg-white/[0.05] p-4"><h2 className="mb-4 text-lg font-semibold text-white">{title}</h2><img src={`${API_URL}/graphs/${file}`} alt={title} className="min-h-64 w-full rounded-md border border-white/10 bg-white object-contain" onError={(event)=>{event.currentTarget.style.display="none";}}/></article>)}</div><article className="mt-6 rounded-lg border border-white/10 bg-white/[0.05] p-5"><h2 className="text-xl font-semibold text-white">Classification Report</h2>{report?<pre className="mt-4 overflow-x-auto rounded-lg bg-slate-950 p-4 text-sm leading-6 text-slate-200">{report}</pre>:<div className="mt-4 flex items-center gap-3 rounded-lg border border-amber-300/20 bg-amber-300/10 p-4 text-amber-100"><ImageOff size={20}/>{error||"Loading report..."}</div>}</article></section>;}
